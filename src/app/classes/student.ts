import { Group } from "../classes/group";

export class Student {
    idS!: number;
    firstName!: String;
    lastName!: String;
    email!: String;
    group!: Group;
    password!: String;
    accessCode!: number;
}
export const Level=[1,2,3];

